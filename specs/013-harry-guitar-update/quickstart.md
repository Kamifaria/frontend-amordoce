# Quickstart: Harry's Guitar Minigame Update

1. Review the plan in `plan.md`.
2. Review the data model changes in `data-model.md` to see how `GuitarMinigame` state will be modified.
3. Review the technical rationale in `research.md`.
4. Proceed to execute the plan by running `/speckit-tasks` to generate the tasks checklist, or simply implement the changes in `GuitarMinigame.tsx`.
