# Data Model: Dialogue Modifications

No modifications to the TypeScript type definitions are required. The existing interfaces defined in `src/shared/types.ts` (`DialogueNode` and `Choice`) are sufficient. We will only be populating new data nodes inside `src/mock/storyData.ts`.
